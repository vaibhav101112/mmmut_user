<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.vaibhavguptammmut.mmmut">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/mmut"
        android:label="@string/app_name"
        android:roundIcon="@drawable/mmut"
        android:supportsRtl="true"
        android:theme="@style/Theme.MMMUT"
        android:name=".CollegeApp">
        <activity android:name=".authentication.ForgetPasswordActivity"></activity>
        <activity android:name=".authentication.LoginActivity" />
        <activity android:name=".authentication.RegisterActivity"></activity>
        <activity android:name=".FullImageView" />
        <activity android:name=".ebook.PdfViewerActivity" />
        <activity
            android:name=".ebook.EbookActivity"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>